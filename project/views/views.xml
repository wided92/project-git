<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="project.list">
      <field name="name">project list</field>
      <field name="model">project.project</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="project.action_window">
      <field name="name">project window</field>
      <field name="res_model">project.project</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="project.action_server">
      <field name="name">project server</field>
      <field name="model_id" ref="model_project_project"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="project" id="project.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="project.menu_1" parent="project.menu_root"/>
    <menuitem name="Menu 2" id="project.menu_2" parent="project.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="project.menu_1_list" parent="project.menu_1"
              action="project.action_window"/>
    <menuitem name="Server to list" id="project" parent="project.menu_2"
              action="project.action_server"/>
    -->
  </data>
</odoo>